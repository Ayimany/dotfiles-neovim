return {
    ---------------------------------------------------------------------------

    'rcarriga/nvim-notify',

    ---------------------------------------------------------------------------

    event = 'VeryLazy',

    ---------------------------------------------------------------------------

    opts = {
        background_colour = '#1e1e2e',
    },

    ---------------------------------------------------------------------------

    config = function(_, opts)
        local notify = require('notify')
        notify.setup(opts)
        vim.notify = notify
    end,

    ---------------------------------------------------------------------------
}
